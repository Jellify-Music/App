diff --git a/node_modules/react-native-drax/src/DraxProvider.tsx b/node_modules/react-native-drax/src/DraxProvider.tsx
index 75580b2..d2c3683 100644
--- a/node_modules/react-native-drax/src/DraxProvider.tsx
+++ b/node_modules/react-native-drax/src/DraxProvider.tsx
@@ -1,6 +1,5 @@
 import type { ReactNode, RefObject } from 'react';
 import { useCallback, useMemo, useRef, useState } from 'react';
-import type { HostInstance } from 'react-native';
 import { StyleSheet, View } from 'react-native';
 import { useSharedValue } from 'react-native-reanimated';
 
@@ -106,20 +105,16 @@ export const DraxProvider = ({
     });
 
   // ── Root view ref ──────────────────────────────────────────────────
-  const rootViewRef = useRef<HostInstance>(null);
-  const setRootViewRef = (ref: HostInstance | null) => {
-    rootViewRef.current = ref;
-  };
+  const rootViewRef = useRef<View>(null);
 
   // Measure root view's screen position on layout
   const handleRootLayout = useCallback(() => {
-    const view = rootViewRef.current;
-    if (view) {
-      (view as unknown as { measure: (cb: (...args: number[]) => void) => void })
-        .measure((_x, _y, _w, _h, pageX, pageY) => {
-          rootOffsetSV.value = { x: pageX, y: pageY };
+      const view = rootViewRef.current;
+      if (view) {
+        view.measureInWindow((x, y) => {
+          rootOffsetSV.value = { x, y };
         });
-    }
+      }
   }, [rootOffsetSV]);
 
   // ── Stable context value ───────────────────────────────────────────
@@ -194,7 +189,7 @@ export const DraxProvider = ({
 
   return (
     <DraxContext value={contextValue}>
-      <View style={style} ref={setRootViewRef} onLayout={handleRootLayout} collapsable={false}>
+      <View style={style} ref={rootViewRef} onLayout={handleRootLayout} collapsable={false}>
         {children}
         {debug && (
           <DebugOverlay
diff --git a/node_modules/react-native-drax/src/DraxView.tsx b/node_modules/react-native-drax/src/DraxView.tsx
index 31a0c65..401359a 100644
--- a/node_modules/react-native-drax/src/DraxView.tsx
+++ b/node_modules/react-native-drax/src/DraxView.tsx
@@ -177,7 +177,7 @@ export const DraxView = memo((props: DraxViewProps): ReactNode => {
   const parentViewRef = parent ? parent.viewRef : rootViewRef;
 
   // View ref for measuring
-  const viewRef = useRef<ComponentRef<typeof Reanimated.View>>(null);
+  const viewRef = useRef<Reanimated.View>(null);
   const measurementsRef = useRef<DraxViewMeasurements | undefined>(undefined);
 
   // ── Measurement ────────────────────────────────────────────────────
diff --git a/node_modules/react-native-drax/src/HoverLayer.tsx b/node_modules/react-native-drax/src/HoverLayer.tsx
index 41031b3..d8c4e6c 100644
--- a/node_modules/react-native-drax/src/HoverLayer.tsx
+++ b/node_modules/react-native-drax/src/HoverLayer.tsx
@@ -87,7 +87,7 @@ export const HoverLayer = memo(
     // Visual style: applied to the inner content wrapper.
     // Handles user hover styles (border, shadow, scale, rotate, etc.)
     // so they apply to the content bounds, not the full-screen container.
-    const visualStyle = useAnimatedStyle(() => {
+    const visualStyle = useAnimatedStyle<ViewStyle>(() => {
       const phase = dragPhaseSV.value;
       if (phase === 'idle') {
         return {};
@@ -116,8 +116,8 @@ export const HoverLayer = memo(
 
       return {
         ...restStyles,
-        ...(userTransform ? { transform: userTransform as { [key: string]: number }[] } : {}),
-      };
+        ...(userTransform ? { transform: userTransform } : {}),
+      } as ViewStyle;
     });
 
     // Animated dimensions for the inner content wrapper.
@@ -139,7 +139,11 @@ export const HoverLayer = memo(
     // flash (the view renders at default position before useAnimatedStyle kicks in).
     return (
       <Reanimated.View
-        style={[styles.container, positionStyle]}
+        style={[
+          styles.container, 
+          positionStyle,
+          StyleSheet.absoluteFill
+        ]}
         pointerEvents="none"
       >
         <Reanimated.View style={[styles.content, dimensionStyle, visualStyle]}>
@@ -152,7 +156,6 @@ export const HoverLayer = memo(
 
 const styles = StyleSheet.create({
   container: {
-    ...StyleSheet.absoluteFillObject,
     // Default hidden — useAnimatedStyle overrides to opacity:1 when dragging.
     // Prevents a one-frame flash on first mount before the animated style evaluates.
     opacity: 0,
diff --git a/node_modules/react-native-drax/src/types.ts b/node_modules/react-native-drax/src/types.ts
index f393ffc..53a2994 100644
--- a/node_modules/react-native-drax/src/types.ts
+++ b/node_modules/react-native-drax/src/types.ts
@@ -4,10 +4,10 @@ import type {
   NativeSyntheticEvent,
   ScrollViewProps,
   StyleProp,
+  View,
   ViewProps,
   ViewStyle,
 } from 'react-native';
-import type { HostInstance } from 'react-native';
 import type {
   AnimatedStyle,
   EntryOrExitLayoutType,
@@ -517,7 +517,7 @@ export interface DraxContextValue {
   droppedItemsRef: RefObject<Map<string, Set<string>>>;
 
   // ── View refs ──────────────────────────────────────────────────────
-  rootViewRef: { current: HostInstance | null };
+  rootViewRef: RefObject<View | null>;
   parent?: DraxParentView;
 }
 
