diff --git a/node_modules/react-native-nitro-player/android/src/main/java/com/margelo/nitro/nitroplayer/core/TrackPlayerQueueBuild.kt b/node_modules/react-native-nitro-player/android/src/main/java/com/margelo/nitro/nitroplayer/core/TrackPlayerQueueBuild.kt
index ce7948c..f24d336 100644
--- a/node_modules/react-native-nitro-player/android/src/main/java/com/margelo/nitro/nitroplayer/core/TrackPlayerQueueBuild.kt
+++ b/node_modules/react-native-nitro-player/android/src/main/java/com/margelo/nitro/nitroplayer/core/TrackPlayerQueueBuild.kt
@@ -31,7 +31,6 @@ internal fun TrackPlayerCore.rebuildQueueAndPlayFromIndex(index: Int) {
     exo.clearMediaItems()
     exo.setMediaItems(mediaItems)
     exo.seekToDefaultPosition(0)
-    exo.playWhenReady = true
     exo.prepare()
 }
 
@@ -44,7 +43,10 @@ internal fun TrackPlayerCore.rebuildQueueFromCurrentPosition() {
 
     // If current track was removed from the playlist, jump to best substitute
     val currentTrackId = exo.currentMediaItem?.mediaId?.let { extractTrackId(it) }
-    if (currentTrackId != null && currentTracks.none { it.id == currentTrackId }) {
+    if (
+        currentTrackId != null && 
+        currentTracks.none { it.id == currentTrackId } && 
+        currentTemporaryType == TrackPlayerCore.TemporaryType.NONE) {
         if (currentTracks.isEmpty()) return
         playFromIndexInternal(minOf(currentTrackIndex, currentTracks.size - 1))
         return
@@ -54,7 +56,7 @@ internal fun TrackPlayerCore.rebuildQueueFromCurrentPosition() {
 
     // playNext stack — skip index 0 if it is the currently playing item
     if (currentTemporaryType == TrackPlayerCore.TemporaryType.PLAY_NEXT && playNextStack.size > 1) {
-        newQueueTracks.addAll(playNextStack.subList(1, playNextStack.size))
+        newQueueTracks.addAll(playNextStack.subList(0, playNextStack.size - 1))
     } else if (currentTemporaryType != TrackPlayerCore.TemporaryType.PLAY_NEXT) {
         newQueueTracks.addAll(playNextStack)
     }
diff --git a/node_modules/react-native-nitro-player/android/src/main/java/com/margelo/nitro/nitroplayer/core/TrackPlayerTempQueue.kt b/node_modules/react-native-nitro-player/android/src/main/java/com/margelo/nitro/nitroplayer/core/TrackPlayerTempQueue.kt
index e1a8baf..3d1a865 100644
--- a/node_modules/react-native-nitro-player/android/src/main/java/com/margelo/nitro/nitroplayer/core/TrackPlayerTempQueue.kt
+++ b/node_modules/react-native-nitro-player/android/src/main/java/com/margelo/nitro/nitroplayer/core/TrackPlayerTempQueue.kt
@@ -28,6 +28,16 @@ suspend fun TrackPlayerCore.loadPlaylist(playlistId: String) = withPlayerContext
  */
 fun TrackPlayerCore.updatePlaylist(playlistId: String) {
     if (currentPlaylistId != playlistId) return
+
+    val playlist = playlistManager.getPlaylist(playlistId) ?: return
+    val currentTrackId = currentTracks.getOrNull(currentTrackIndex)?.id
+    if (currentTrackId != null) {
+        val resolvedIndex = playlist.tracks.indexOfFirst { it.id == currentTrackId }
+        if (resolvedIndex >= 0 && resolvedIndex != currentTrackIndex) {
+            currentTrackIndex = resolvedIndex
+        }
+    }
+
     playerHandler.removeCallbacks(updateCurrentPlaylistRunnable)
     playerHandler.post(updateCurrentPlaylistRunnable)
 }
diff --git a/node_modules/react-native-nitro-player/ios/core/TrackPlayerQueue.swift b/node_modules/react-native-nitro-player/ios/core/TrackPlayerQueue.swift
index 766a1af..2140d96 100644
--- a/node_modules/react-native-nitro-player/ios/core/TrackPlayerQueue.swift
+++ b/node_modules/react-native-nitro-player/ios/core/TrackPlayerQueue.swift
@@ -86,16 +86,32 @@ extension TrackPlayerCore {
     // Add current track (temp or original)
     if let current = getCurrentTrack() { queue.append(current) }
 
-    // Add playNext stack — skip index 0 if current is from playNext (already added as current)
+    // Add playNext stack — when current is from playNext, exclude the currently playing item by ID.
     if currentTemporaryType == .playNext && playNextStack.count > 1 {
-      queue.append(contentsOf: playNextStack.dropFirst())
+      if let currentTrackId = player?.currentItem?.trackId {
+        var remainingPlayNext = playNextStack
+        if let playingIndex = remainingPlayNext.firstIndex(where: { $0.id == currentTrackId }) {
+          remainingPlayNext.remove(at: playingIndex)
+        }
+        queue.append(contentsOf: remainingPlayNext)
+      } else {
+        queue.append(contentsOf: playNextStack)
+      }
     } else if currentTemporaryType != .playNext {
       queue.append(contentsOf: playNextStack)
     }
 
-    // Add upNext queue — skip index 0 if current is from upNext (already added as current)
+    // Add upNext queue — when current is from upNext, exclude the currently playing item by ID.
     if currentTemporaryType == .upNext && upNextQueue.count > 1 {
-      queue.append(contentsOf: upNextQueue.dropFirst())
+      if let currentTrackId = player?.currentItem?.trackId {
+        var remainingUpNext = upNextQueue
+        if let playingIndex = remainingUpNext.firstIndex(where: { $0.id == currentTrackId }) {
+          remainingUpNext.remove(at: playingIndex)
+        }
+        queue.append(contentsOf: remainingUpNext)
+      } else {
+        queue.append(contentsOf: upNextQueue)
+      }
     } else if currentTemporaryType != .upNext {
       queue.append(contentsOf: upNextQueue)
     }
@@ -115,6 +131,13 @@ extension TrackPlayerCore {
       if currentTemporaryType == .playNext { return playNextStack.first(where: { $0.id == trackId }) }
       if currentTemporaryType == .upNext { return upNextQueue.first(where: { $0.id == trackId }) }
     }
+
+    // Prefer AVQueuePlayer's current item as source of truth in playlist mode.
+    if let activeTrackId = player?.currentItem?.trackId,
+      let activeTrack = currentTracks.first(where: { $0.id == activeTrackId }) {
+      return activeTrack
+    }
+
     guard currentTrackIndex >= 0 && currentTrackIndex < currentTracks.count else { return nil }
     return currentTracks[currentTrackIndex]
   }
diff --git a/node_modules/react-native-nitro-player/ios/core/TrackPlayerQueueBuild.swift b/node_modules/react-native-nitro-player/ios/core/TrackPlayerQueueBuild.swift
index 36fa6cf..469c1bc 100644
--- a/node_modules/react-native-nitro-player/ios/core/TrackPlayerQueueBuild.swift
+++ b/node_modules/react-native-nitro-player/ios/core/TrackPlayerQueueBuild.swift
@@ -216,13 +216,16 @@ extension TrackPlayerCore {
     guard let player = self.player else { return }
 
     let currentItem = player.currentItem
+      
+    guard let playingTrackId = currentItem?.trackId else { return }
+      
     let playingItems = player.items()
 
     // If the currently playing AVPlayerItem is no longer in currentTracks,
     // delegate to rebuildQueueFromPlaylistIndex so the player immediately
     // starts what is now at currentTrackIndex in the updated list.
-    if let playingTrackId = currentItem?.trackId,
-      !currentTracks.contains(where: { $0.id == playingTrackId }) {
+    if !currentTracks.contains(where: { $0.id == playingTrackId }) &&
+      currentTemporaryType == .none {
       let targetIndex = currentTrackIndex < currentTracks.count
         ? currentTrackIndex : currentTracks.count - 1
       if targetIndex >= 0 {
@@ -235,13 +238,25 @@ extension TrackPlayerCore {
     var newQueueTracks: [TrackItem] = []
 
     if currentTemporaryType == .playNext && playNextStack.count > 1 {
-      newQueueTracks.append(contentsOf: playNextStack.dropFirst())
+      if let playingIndex = playNextStack.firstIndex(where: { $0.id == playingTrackId }) {
+        var remainingPlayNext = playNextStack
+        remainingPlayNext.remove(at: playingIndex)
+        newQueueTracks.append(contentsOf: remainingPlayNext)
+      } else {
+        newQueueTracks.append(contentsOf: playNextStack)
+      }
     } else if currentTemporaryType != .playNext {
       newQueueTracks.append(contentsOf: playNextStack)
     }
 
     if currentTemporaryType == .upNext && upNextQueue.count > 1 {
-      newQueueTracks.append(contentsOf: upNextQueue.dropFirst())
+      if let playingIndex = upNextQueue.firstIndex(where: { $0.id == playingTrackId }) {
+        var remainingUpNext = upNextQueue
+        remainingUpNext.remove(at: playingIndex)
+        newQueueTracks.append(contentsOf: remainingUpNext)
+      } else {
+        newQueueTracks.append(contentsOf: upNextQueue)
+      }
     } else if currentTemporaryType != .upNext {
       newQueueTracks.append(contentsOf: upNextQueue)
     }
diff --git a/node_modules/react-native-nitro-player/ios/core/TrackPlayerTempQueue.swift b/node_modules/react-native-nitro-player/ios/core/TrackPlayerTempQueue.swift
index ee1fc26..43fe5bc 100644
--- a/node_modules/react-native-nitro-player/ios/core/TrackPlayerTempQueue.swift
+++ b/node_modules/react-native-nitro-player/ios/core/TrackPlayerTempQueue.swift
@@ -42,6 +42,13 @@ extension TrackPlayerCore {
 
   func updatePlaylist(playlistId: String) {
     guard currentPlaylistId == playlistId else { return }
+    guard let playlist = playlistManager.getPlaylist(playlistId: playlistId) else { return }
+
+    if let currentTrackId = player?.currentItem?.trackId,
+      let resolvedIndex = playlist.tracks.firstIndex(where: { $0.id == currentTrackId }),
+      resolvedIndex != currentTrackIndex {
+      currentTrackIndex = resolvedIndex
+    }
 
     // Cancel any pending rebuild so back-to-back calls collapse into a single rebuild.
     pendingPlaylistUpdateWorkItem?.cancel()
